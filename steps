# clean the code   //done

# convert struct to class //done

# replace client struct with vector or map struct //done

manage multiple clients(multiplexing) // done

make some private attributes if I can

choose a reference client and test based on it

follow the subject and add some rules(operators, commands ...)
{
    create vector of channels
    setnickname
    username
    join channell
    /pm private message
}

compare the behavior of my server with the behavior of an existing one


add exceptions to handle errors

password ?

parse arguments

separate functions 